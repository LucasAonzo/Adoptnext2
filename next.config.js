/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'lwtrjbglrdehtwaxunop.supabase.co',
      'images.unsplash.com'
    ],
  },
};

module.exports = nextConfig;